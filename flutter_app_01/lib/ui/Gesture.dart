import 'package:flutter/material.dart';

class Gesture extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    final String title = "Gesture control";
    return new Scaffold(

    );
  }


}